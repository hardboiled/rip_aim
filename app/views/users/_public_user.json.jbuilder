# basically a duplicate of #show, but left
# separate because users may have private data in the future
json.id user.id
json.username user.username
json.created_at user.created_at
json.updated_at user.updated_at
